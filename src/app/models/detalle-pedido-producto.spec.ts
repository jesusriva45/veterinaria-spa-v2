import { DetallePedidoProducto } from './detalle-pedido-producto';

describe('DetallePedidoProducto', () => {
  it('should create an instance', () => {
    expect(new DetallePedidoProducto()).toBeTruthy();
  });
});
