import { DetallePedidoServicio } from './detalle-pedido-servicio';

describe('DetallePedidoServicio', () => {
  it('should create an instance', () => {
    expect(new DetallePedidoServicio()).toBeTruthy();
  });
});
